@model IEnumerable<student_attendance.Models.UserAllRole>

@{
    ViewBag.Title = "Index";
}

<h2>User With Roles</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.role_id_fk.role)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.user_id_fk.user_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.role_id_fk.role)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.user_id_fk.user_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.user_role_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.user_role_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.user_role_id })
        </td>
    </tr>
}

</table>
